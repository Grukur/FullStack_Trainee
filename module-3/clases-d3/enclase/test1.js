
//DEFINIR DESCUENTO POR TRAMO

/* let totalCompra = parseInt(prompt('Ingrese el total de su compra'));
let precioFinal;

if (totalCompra < 50000) {
alert('sin dcto :(');
precioFinal = totalCompra;

} else if (totalCompra >= 50000 && totalCompra < 100000) {
alert('dcto 5%');
precioFinal = totalCompra * 0.95;

} else if (totalCompra >= 100000 && totalCompra < 200000) {
alert('dcto 10%');
precioFinal = totalCompra * 0.9;

} else {
alert('dcto 15%');
precioFinal = totalCompra * 0.85;
}

alert(`total de la compra : ${totalCompra}`);
alert(`Precio final a pagar: ${precioFinal}`); */

/* promedio */
/* $('#button').click(function () {
    const askCalifications = () => {
        let userInput = parseInt(prompt('por favor ingrese cuantas notas va a ingresar'));
        arrayCalifications = [];

        for (let i = 1; i <= userInput; i++) {
            let califications = parseFloat(prompt(`por ingrese su nota ${i}: `))
            arrayCalifications.push(califications)
        } alert(average(arrayCalifications, userInput))
    }

    const average = (array, total) => {
        const initial = 0;
        const sum = array.reduce((accumulator, currentV) => accumulator + currentV, initial)
        return ((sum / total).toFixed(2))
    }
    askCalifications()
}) */


/* crear funcion que reciba un array y retorne el numero mayor */

/*  */

